services:
  commander:
    build:
      context: ..
      dockerfile: ai/Dockerfile
    restart: "on-failure:5"
    env_file:
      - ../.env
    volumes:
      - ./data:/app/ai/data
    command: ["python", "-m", "commander.main", "watch"]
